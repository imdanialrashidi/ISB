---
import SectionTitle from "../components/SectionTitle.astro";
import WhatsAppForm from "../components/WhatsAppForm.astro";
import contactsData from "../content/contacts.json";
import BaseLayout from "../layouts/BaseLayout.astro";
import { compactWebsiteLabel, normalizePhoneForDisplay, toPersianDigits } from "../lib/locale";
import { waMeHref } from "../lib/whatsapp";
import type { ContactsContent } from "../lib/types";

const contacts = contactsData as ContactsContent;
const mainPhone = contacts.phones[0];
const mobilePhone = contacts.phones.find((phone) => phone.tel.includes("98912")) ?? contacts.phones[1];
const whatsappDigits = contacts.whatsapp.number.replace(/[^\d]/g, "");
const whatsappActive = contacts.whatsapp.status === "active";
const whatsappHref = whatsappActive && whatsappDigits ? waMeHref(whatsappDigits) : "/contact#whatsapp";
const whatsappDisplay = toPersianDigits(normalizePhoneForDisplay(whatsappDigits));
const websiteLabel = compactWebsiteLabel(contacts.website);
const mapOffice =
  contacts.addresses.find((address) => address.city.includes("تهران")) ?? contacts.addresses[0];
const activeMessaging = contacts.messaging.filter((channel) => channel.status === "active");
const pendingMessaging = contacts.messaging.filter(
  (channel) => channel.status === "missing_identifier",
);
---

<BaseLayout
  title="تماس با شرکت"
  description="اطلاعات تماس دفتر مرکزی شرکت ایمن صنعت باتاب، شماره‌های تماس، مسیرهای ارتباطی و نقشه دسترسی."
  canonicalPath="/contact"
>
  <section class="section-block">
    <div class="container-wrap">
      <SectionTitle
        title="تماس با ایمن صنعت باتاب"
        subtitle="برای هماهنگی خدمات، دریافت مشاوره یا استعلام همکاری با واحد پشتیبانی شرکت در ارتباط باشید."
      />

      <div class="grid gap-6 lg:grid-cols-2">
        <article class="card p-6">
          <h2 class="text-xl font-bold text-brand-primary">اطلاعات تماس</h2>
          <ul class="mt-4 space-y-3 text-sm text-brand-text">
            {
              contacts.phones.map((phone) => (
                <li>
                  <a href={`tel:${phone.tel}`} class="font-semibold hover:text-brand-accent">
                    {phone.label}: <span class="num-text">{toPersianDigits(phone.number)}</span>
                  </a>
                </li>
              ))
            }
            <li>
              فکس: <span class="num-text">{toPersianDigits(contacts.fax)}</span>
            </li>
            <li>
              <a href={`mailto:${contacts.email}`} class="hover:text-brand-accent">
                ایمیل: <span class="latin-text">{contacts.email}</span>
              </a>
            </li>
            <li>
              <a href={contacts.website} class="hover:text-brand-accent">
                وب‌سایت: <span class="latin-text">{websiteLabel}</span>
              </a>
            </li>
            <li>
              <a
                href={whatsappHref}
                target={whatsappHref.startsWith("https://wa.me/") ? "_blank" : undefined}
                rel={whatsappHref.startsWith("https://wa.me/") ? "noopener noreferrer" : undefined}
                class="hover:text-brand-accent"
              >
                واتساپ: {whatsappActive ? whatsappDisplay : contacts.whatsapp.fallbackMessage || "در حال پیگیری"}
              </a>
            </li>
          </ul>
          <p
            class="mt-4 rounded-lg border border-brand-border bg-slate-50 px-3 py-2 text-xs text-brand-muted"
          >
            {contacts.workingHours}
          </p>
          <div class="mt-5 rounded-xl border border-brand-border bg-slate-50 p-4">
            <p class="text-sm font-semibold text-brand-primary">تماس فوری</p>
            <div class="mt-3 grid gap-2 sm:grid-cols-2">
              <a
                href={`tel:${mainPhone.tel}`}
                class="inline-flex items-center justify-center gap-2 rounded-full bg-brand-accent px-4 py-2.5 text-sm font-semibold text-white hover:bg-emerald-700"
              >
                دفتر: <span class="num-text">{toPersianDigits(mainPhone.number)}</span>
              </a>
              <a
                href={`tel:${mobilePhone.tel}`}
                class="inline-flex items-center justify-center gap-2 rounded-full border border-brand-accent bg-white px-4 py-2.5 text-sm font-semibold text-brand-accent hover:bg-brand-accent hover:text-white"
              >
                همراه: <span class="num-text">{toPersianDigits(mobilePhone.number)}</span>
              </a>
            </div>
          </div>
        </article>

        <article class="card p-6">
          <h2 class="text-xl font-bold text-brand-primary">آدرس دفتر</h2>
          <ul class="mt-4 space-y-4 text-sm leading-7 text-brand-text">
            {
              contacts.addresses.map((address) => (
                <li class="rounded-xl border border-brand-border p-3">
                  <p class="font-semibold text-brand-primary">{address.city}</p>
                  <p class="mt-1">{address.fullAddress}</p>
                  <a
                    href={address.mapUrl}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="mt-2 inline-flex text-xs font-semibold text-brand-accent hover:text-brand-primary"
                  >
                    مشاهده روی نقشه
                  </a>
                </li>
              ))
            }
          </ul>
        </article>
      </div>

      {mapOffice?.embedUrl && (
        <article class="card mt-8 overflow-hidden p-2">
          <h2 class="px-4 pt-4 text-lg font-bold text-brand-primary">نقشه دفتر مرکزی تهران</h2>
          <p class="px-4 pt-2 text-sm text-brand-muted">{mapOffice.fullAddress}</p>
          <iframe
            title="نقشه دفتر مرکزی ایمن صنعت باتاب"
            src={mapOffice.embedUrl}
            loading="lazy"
            referrerpolicy="no-referrer-when-downgrade"
            class="mt-4 h-[360px] w-full rounded-xl border border-brand-border"
          ></iframe>
        </article>
      )}

      <div class="mt-8">
        <WhatsAppForm whatsapp={contacts.whatsapp} />
      </div>

      <div class="mt-6">
        <article class="card p-6">
          <h2 class="text-xl font-bold text-brand-primary">مسیرهای پیام‌رسان</h2>
          <p class="mt-3 text-sm leading-7 text-brand-muted">
            از طریق پیام‌رسان‌های زیر نیز می‌توانید با شرکت در ارتباط باشید.
          </p>
          <ul class="mt-4 flex flex-wrap gap-3">
            {
              activeMessaging.map((channel) => (
                <li>
                  <a
                    href={channel.url}
                    target={channel.url.startsWith("http") ? "_blank" : undefined}
                    rel={channel.url.startsWith("http") ? "noopener noreferrer" : undefined}
                    class="inline-flex items-center gap-2 rounded-full bg-brand-accent px-5 py-2.5 text-sm font-semibold text-white hover:bg-brand-primary"
                  >
                    {channel.label}
                    {channel.number && (
                      <span class="num-text text-xs text-white/85">{toPersianDigits(channel.number)}</span>
                    )}
                  </a>
                </li>
              ))
            }
            {
              pendingMessaging.map((channel) => (
                <li class="inline-flex items-center gap-2 rounded-full border border-brand-border bg-slate-50 px-5 py-2.5 text-sm font-semibold text-brand-muted">
                  {channel.label}
                  <span class="text-xs font-normal">{channel.note}</span>
                </li>
              ))
            }
          </ul>
        </article>
      </div>
    </div>
  </section>
</BaseLayout>
