---
import BaseLayout from "../layouts/BaseLayout.astro";
import { toPersianDigits } from "../lib/locale";
---

<BaseLayout
  title="صفحه پیدا نشد"
  description="صفحه مورد نظر شما در وب‌سایت ایمن صنعت باتاب پیدا نشد."
  canonicalPath="/404"
  noIndex={true}
>
  <section class="section-block">
    <div class="container-wrap">
      <article class="card p-8 text-center">
        <p class="text-6xl font-bold text-brand-primary">{toPersianDigits("404")}</p>
        <h1 class="mt-4 text-2xl font-bold text-brand-primary">صفحه مورد نظر پیدا نشد</h1>
        <p class="mt-3 text-sm text-brand-muted">
          آدرس وارد شده صحیح نیست یا صفحه جابه‌جا شده است.
        </p>
        <div class="mt-6 flex flex-wrap justify-center gap-3">
          <a
            href="/"
            class="rounded-full bg-brand-accent px-5 py-2.5 text-sm font-semibold text-white"
          >
            بازگشت به خانه
          </a>
          <a
            href="/contact"
            class="rounded-full border border-brand-primary px-5 py-2.5 text-sm font-semibold text-brand-primary"
          >
            تماس با ما
          </a>
        </div>
      </article>
    </div>
  </section>
</BaseLayout>
